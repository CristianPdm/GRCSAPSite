# Dependencias adicionales SOLO para el servidor Linux de produccion.
# No se instalan en el entorno de desarrollo en Windows porque gunicorn
# no funciona ahi (depende de modulos de Unix).
#
# Instalar junto con requirements.txt:
#   pip install -r requirements.txt -r requirements-prod.txt

gunicorn==23.0.0
